ROS 项目基础目标

我给机器人一个目标点，任务节点能接收任务、触发执行、拿到结果，并输出任务状态。

要求：生成的代码要有注释，且著名为什么要这么实现

1.建立 ROS2 项目骨架

  创建这些模块：

  task_manager
  navigator_bridge
  state_manager
  logger
  目标：
  
  项目结构清晰
  每个模块职责明确

2. 写任务管理节点

实现：

接收目标点
保存当前任务
触发执行
接收完成/失败结果

目标：

能处理一条任务
3. 定义任务状态

至少有这几个状态：

Idle
Pending
Running
Arrived
Failed

目标：

状态流转清楚
控制台能打印状态变化
4. 打通执行链路

实现流程：

输入目标点
任务节点接收
调用导航执行
返回结果
更新状态

目标：

机器人能真正响应一次任务
5. 加基础日志

记录：

任务开始
任务目标点
当前状态变化
成功或失败

目标：

出问题时能看日志定位
6. 做最小演示

准备一个演示流程：

启动系统
发一个目标点
机器人执行
打印结果

目标：

能录一段 30 到 60 秒视频